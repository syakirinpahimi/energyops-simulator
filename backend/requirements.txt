# Backend runtime + tooling deps.
# Pinned to known-good versions for reproducibility.

# --- web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- config ---
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# --- database ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# --- security ---
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# --- mqtt + reports (used by other backend tasks; harmless here) ---
paho-mqtt==2.1.0
reportlab==4.2.5

# --- dev / tests ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
